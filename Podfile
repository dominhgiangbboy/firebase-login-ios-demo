# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LoginPageFirebase' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LoginPageFirebase
  pod 'Alamofire', '~> 5.4'
  
  target 'LoginPageFirebaseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LoginPageFirebaseUITests' do
    # Pods for testing
  end

end
